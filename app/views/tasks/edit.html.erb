<h1>タスク:<%=@task.content %> 編集ページ</h1>
<%= form_with(model: @task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '更新' %>
<% end %>
<br>
<%= link_to '一覧に戻る', tasks_path %>